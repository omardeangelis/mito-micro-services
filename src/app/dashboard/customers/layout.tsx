import React from "react"

const CustomerLayout = ({ children }: { children: React.ReactNode }) => {
  return <section className="dashabord-container">{children}</section>
}

export default CustomerLayout
