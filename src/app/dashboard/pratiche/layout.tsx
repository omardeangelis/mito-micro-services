import React from "react"

const PraticheLayout = ({ children }: { children: React.ReactNode }) => {
  return <section className="dashabord-container">{children}</section>
}

export default PraticheLayout
